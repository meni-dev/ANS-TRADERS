# The shell must never be cached, or a deploy leaves people on the old app pointing at asset files
# that no longer exist. Everything under /assets carries a content hash in its name, so it can be
# cached hard and forever.
/index.html
  Cache-Control: no-cache

/assets/*
  Cache-Control: public, max-age=31536000, immutable
